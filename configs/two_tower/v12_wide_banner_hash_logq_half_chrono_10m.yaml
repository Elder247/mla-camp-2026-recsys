extends: v8_logq_half_fast_base_chrono_10m.yaml

experiment:
  name: two_tower_v12_wide_banner_hash_logq_half_chrono_10m

paths:
  artifact_dir: /home/astrofimuk/workspace/mla_two_stage/artifacts/two_tower_v12_wide_banner_hash_logq_half_chrono_10m_model

tracking:
  underdeep:
    run_name: two-tower-v12-wide-banner-hash-logq-half-chrono-10m

model:
  banner_cardinalities:
    # The indexed catalogue has one million banners; the second identity hash
    # should have comparable capacity instead of forcing four items per bucket.
    banner_id_hash2_ids: 1048576
